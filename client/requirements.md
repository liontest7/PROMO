## Packages
framer-motion | Smooth animations for page transitions and interactive elements
recharts | Visualization for campaign statistics and earnings
date-fns | Formatting dates for campaign timelines
lucide-react | Iconography (already in base, but heavily used)
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Outfit'", "sans-serif"],
  body: ["'Space Grotesk'", "sans-serif"],
}
